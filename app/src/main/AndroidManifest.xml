<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.movlix">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".utils.MovlixApp"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Movlix"
        tools:targetApi="m">
        <activity
            android:name=".feature.forgetPassword.view.ForgetPasswordActivity"
            android:exported="true"
            android:theme="@style/status_bar"/>
        <activity
            android:name=".feature.movieDetails.view.MovieDetailsActivity"
            android:exported="true"
            android:theme="@style/status_bar" />
        <activity
            android:name=".feature.homeMovies.view.HomeMoviesActivity"
            android:exported="true"
            android:theme="@style/status_bar" />
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="true"
            android:theme="@style/whiteThem" />
        <activity
            android:name=".feature.profile.view.ProfileActivity"
            android:exported="true"
            android:theme="@style/status_bar_profile" />
        <activity
            android:name=".activities.OnboardingActivity"
            android:exported="true" />
        <activity
            android:name=".feature.login.view.LoginActivity"
            android:exported="true"
            android:theme="@style/status_bar" />
        <activity
            android:name=".feature.signup.view.SignupActivity"
            android:exported="true"
            android:theme="@style/status_bar">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/status_bar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>