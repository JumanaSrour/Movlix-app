<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movlix">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".utils.MovlixApp"
        android:theme="@style/Theme.Movlix">
        <activity
            android:name=".activities.ForgetPasswordActivity"
            android:exported="true"
            android:theme="@style/status_bar"/>
        <activity
            android:name=".activities.MovieDetailsActivity"
            android:exported="true"
            android:theme="@style/status_bar" />
        <activity
            android:name=".activities.HomeMoviesActivity"
            android:exported="true"
            android:theme="@style/status_bar" />
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="true"
            android:theme="@style/whiteThem" />
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="true"
            android:theme="@style/status_bar_profile" />
        <activity
            android:name=".activities.Onboarding"
            android:exported="true" />
        <activity
            android:name=".feature.login.view.Login"
            android:exported="true"
            android:theme="@style/status_bar" />
        <activity
            android:name=".feature.signup.view.Signup"
            android:exported="true"
            android:theme="@style/status_bar">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Splash"
            android:exported="true"
            android:theme="@style/status_bar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>